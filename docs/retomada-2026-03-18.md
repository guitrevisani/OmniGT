# Retomada — 2026-03-18

---

## Status ao final de 2026-03-17

### Agenda
Pipeline validado end-to-end via webhook real:
- Webhook → worker → dispatcher → bloco na descrição ✓
- Notificação push chegando no device ✓ (Chrome Android)

### Camp
Engine validada end-to-end com atividade real:
- `matchSession` por horário funcionando ✓
- `computeTotals` com acumulados corretos ✓
- `buildDescription` gerando bloco correto ✓
- `estimateNP` / `estimateFTP` / `calculateIF` — IF na faixa esperada (confirmado vs TrainingPeaks) ✓
- NP estimada — sem referência externa disponível para validação

---

## Correções aplicadas em 2026-03-17

### Push notification
- Substituído carregamento manual do SDK por pacote `react-onesignal`
- Adicionado `await` no `sendPushNotification` (fire-and-forget não funciona em serverless)
- Corrigido payload OneSignal: adicionado conteúdo em `en` além de `pt`
- Corrigido `stravaId` ausente na chamada de `sendPushNotification`
- Adicionado delay de 1,5s no `AgendaDashboard.jsx` antes de checar `optedIn`

### Camp — bugs corrigidos
- `activities.strava_route_id` adicionado ao schema e ao UPDATE do worker
- `matchByDatetime` em `matchSession.js`: parâmetro `activityDate` removido da query (não era referenciado, causava erro de tipo no Postgres)

### Migration executada
```sql
ALTER TABLE activities ADD COLUMN strava_route_id bigint;
```

---

## Ambiente de teste do Camp (banco)

| Recurso | Valor |
|---|---|
| event_id | 4 |
| slug | `camp-teste` |
| período | CURRENT_DATE + 3 dias |
| atleta | 23978606 (owner) |
| sessão | Dia 1, ordem 1, "Saída matinal", 07:00 |
| FTP teste | 280W |
| peso teste | 75kg |

---

## Primeira tarefa ao retomar

**1. Comparar bloco de descrição com valores do app de monitoramento**
Subir atividade com dados de potência e FC e comparar IF, NP e zonas
com os valores exibidos pelo TrainingPeaks, Garmin Connect ou similar.

**2. Discutir CTL / ATL / TSB / TSS**
Pontos a decidir:
- Manter CTL e ATL como estão (calculados só com atividades do camp)
- Processar atividades passadas para melhor estimativa de CTL histórico
- Adotar TSS em substituição ou complementação às métricas atuais
- TSS requer IF² × tempo — já temos IF calculado, seria natural incluir

**3. Seguir com implementação do Camp**
Próximos passos em ordem:
- Rotas de API: formulário de inscrição (`/api/camp/[slug]/register`)
- Rotas de API: configuração de sessões pelo owner
- Dashboards individual e owner (ambos exportáveis como imagem)

---

## Estado atual dos arquivos do Camp

Todos commitados e em produção:

| Arquivo | Caminho |
|---|---|
| `index.js` | `src/engine/modules/camp/index.js` |
| `computeTotals.js` | `src/engine/modules/camp/computeTotals.js` |
| `matchSession.js` | `src/engine/modules/camp/matchSession.js` |
| `buildDescription.js` | `src/engine/modules/camp/buildDescription.js` |
| `estimateNP.js` | `src/lib/physics/estimateNP.js` |
| `estimateFTP.js` | `src/lib/physics/estimateFTP.js` |
| `estimateIF.js` | `src/lib/physics/estimateIF.js` |
| `estimateZones.js` | `src/lib/physics/estimateZones.js` |
| `module-dispatcher.route.js` | `src/app/api/internal/module-dispatcher/route.js` |
| `strava-worker.route.js` | `src/app/api/internal/strava-worker/route.js` |

---

## Pendências do Camp

1. **`schedule_tolerance_min` configurável** — hardcoded em 90min em `matchSession.js`. Tornar configurável via `event_configs.metadata.schedule_tolerance_min` com fallback 90.
2. **Rotas de API** — formulário de inscrição, configuração de sessões, dashboards.
3. **`matchByRoute`** — funcional mas sem dados reais para testar (nenhuma atividade tem `strava_route_id` ainda).

---

## Schema — histórico de alterações

```sql
-- 2026-03-15
ALTER TABLE activities ADD COLUMN start_date_local timestamp without time zone;

-- 2026-03-16
CREATE TABLE camp_sessions (...);
CREATE TABLE camp_session_activities (...);
CREATE TABLE camp_athlete_profiles (...);
INSERT INTO modules VALUES ('camp', ...);

-- 2026-03-17
ALTER TABLE activities ADD COLUMN strava_route_id bigint;
```

---

## Problemas conhecidos não resolvidos

### Atividade órfã 17568831088 (28/02)
Marcada como `duplicate_of = 17568650333` manualmente. Sem ação necessária.

### Divergências residuais agenda_daily vs planilha
Diferenças de ~3 km a partir de 03/02 são arredondamentos da planilha manual.
Banco correto. Sem ação necessária.
